# Capability catalog

Gameplay requires explicit configuration opt-in. Each entry states its tested scope; implemented-tested does not mean complete parity or production certification. Linked sources retain real BDS storage and world ticking.

| UI | Status / backend | Commands | Evidence and remaining gap |
|---|---|---|---|
| chest | implemented-unverified / native-linked-block-windows | /workstations open chest, /chest | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| doublechest | implemented-unverified / native-linked-block-windows | /workstations open doublechest, /doublechest | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| hopper | implemented-tested / native-linked-block-windows | /workstations open hopper, /hopper | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| dispenser | implemented-unverified / native-linked-block-windows | /workstations open dispenser, /dispenser | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| barrel | implemented-unverified / native-linked-block-windows | /workstations open barrel, /barrel | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| dropper | implemented-unverified / native-linked-block-windows | /workstations open dropper, /dropper | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| trappedchest | implemented-unverified / native-linked-block-windows | /workstations open trappedchest, /trappedchest | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| enderchest | implemented-tested / packet-real-ender-windows | /workstations open enderchest, /enderchest, /ec | Real online Ender slots support tested deposit, withdrawal, cursor cancellation and external-change rejection through the packet adapter. Use open_ender_chest(); the native-only route still rejects remote transfers. Drops, item-specific descriptor extensions and crash/save atomicity remain unqualified. |
| shulker | unsupported / none | /workstations open shulker, /shulker | The requested source is the actual held box. Public ItemStack NBT alone cannot lock its durable identity across movement, drops, nesting, transfer and BDS saves. No verified native held-box manager or crash-safe escrow/writeback exists; opening a separate vault would violate the source contract. |
| craft | implemented-tested / native-bds-windows | /workstations open craft, /craft, /workbench | Native 3x3 recipe-book crafting consumed 16 planks for two chests; repeated crafting retained one excess plank. Behavior-pack overrides, remainders and full-inventory cases remain unverified. |
| anvil | implemented-tested / native-bds-windows | /workstations open anvil, /anvil | Native rename charged one XP level and preserved Sharpness I; zero-XP output was rejected; normal and forced close returned inputs. Repair, enchant merging and prior-work limits remain unverified. |
| grindstone | implemented-tested / native-bds-windows | /workstations open grindstone, /grindstone | Native disenchant removed Sharpness from one iron sword and visibly awarded XP. Curse retention, repair combinations and exact XP distribution remain unverified. |
| smithing | implemented-tested / native-bds-windows | /workstations open smithing, /smithing | Native Netherite upgrade consumed one diamond pickaxe, ingot and template for one Netherite pickaxe. Trims and metadata-rich upgrade combinations remain unverified. |
| stonecutter | implemented-tested / native-bds-windows | /workstations open stonecutter, /stonecutter | Native recipe selection consumed one stone for one stone brick and returned 63 unused stone. Remaining recipes and full-inventory cases remain unverified. |
| loom | implemented-tested / native-bds-windows | /workstations open loom, /loom | Native selected border pattern consumed two banners and two red dyes for two patterned banners. Special pattern items and pattern limits remain unverified. |
| cartography | implemented-tested / native-bds-windows | /workstations open cartography, /cartography | Native scaling consumed one paper, returned unused paper and rendered the scaled real world map. Copying, locking and persistent map-state comparisons remain unverified. |
| enchanting | implemented-unverified / native-linked-block-windows | /workstations open enchanting, /enchanting, /etable | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| furnace | implemented-unverified / native-linked-block-windows | /workstations open furnace, /furnace | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| blastfurnace | implemented-unverified / native-linked-block-windows | /workstations open blastfurnace, /blastfurnace | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| smoker | implemented-unverified / native-linked-block-windows | /workstations open smoker, /smoker | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| brewing | implemented-unverified / native-linked-block-windows | /workstations open brewing, /brewing | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| beacon | implemented-tested / native-linked-block-windows | /workstations open beacon, /beacon | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| crafter | implemented-tested / native-linked-block-windows | /workstations open crafter, /crafter | Opens the real authorized source using BDS inventory/gameplay machinery. Native source identity and selected live operations are recorded; world ticking and source permissions remain required. See the per-entry validation scope. |
| lectern | implemented-tested / native-linked-block-windows | /workstations open lectern | Opens the real lectern book, preserving its NBT and delegating page changes to the guarded native setter. Installed a3 retained page state across reopen and passed manual/forced closure and rapid switching. |
| writtenbook | unsupported / none | /workstations open writtenbook | The actual Player::openBook entry at RVA 0x693c50 returns immediately for a null BlockActor; the verified server path is lectern-only. Held reading is client item interaction. No verified server force-open trigger is bound; book text stays out of diagnostics. |
| bookediting | unsupported / none | /workstations open bookediting | BookEdit 97 identifies a real inventory slot and typed replace/add/delete/swap/finalize operations. It is a client save protocol, not an editor-opening packet. Held writable-book identity, signed-state validation and a server open trigger are not bound. |
| villager | implemented-tested / native-linked-trade-windows | /workstations open villager | Installed a7 completed a real merchant offer with native payment/output, retained the incremented stock use across reopen and restored the player baseline. Native interaction veto also blocked opening. BDS owns offers, customer, profession and trade validation. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| wanderingtrader | implemented-tested / native-linked-trade-windows | /workstations open wanderingtrader | Installed a7 completed a real merchant offer with native payment/output, retained the incremented stock use across reopen and restored the player baseline. Native interaction veto also blocked opening. BDS owns offers, customer, profession and trade validation. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| horse | implemented-tested / native-linked-equipment-windows | /workstations open horse | The installed native backend passed real horse saddle/armor or chested-donkey storage deposit, close/reopen and withdrawal with exact NBT and player baseline restoration. Source ownership and native attachment determine access and layout. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| donkey | implemented-tested / native-linked-equipment-windows | /workstations open donkey | The installed native backend passed real horse saddle/armor or chested-donkey storage deposit, close/reopen and withdrawal with exact NBT and player baseline restoration. Source ownership and native attachment determine access and layout. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| mule | implemented-unverified / native-linked-equipment-windows | /workstations open mule | The native equipment backend is implemented and its actual vanilla screen opened in the development probe. Native ownership, chest attachment and llama strength determine access and slots. This specific equipment variant has no installed item-transfer qualification. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| llama | implemented-unverified / native-linked-equipment-windows | /workstations open llama | The native equipment backend is implemented and its actual vanilla screen opened in the development probe. Native ownership, chest attachment and llama strength determine access and slots. This specific equipment variant has no installed item-transfer qualification. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| traderllama | implemented-unverified / native-linked-equipment-windows | /workstations open traderllama | The native equipment backend is implemented and its actual vanilla screen opened in the development probe. Native ownership, chest attachment and llama strength determine access and slots. This specific equipment variant has no installed item-transfer qualification. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| chestboat | implemented-tested / native-linked-entity-windows | /workstations open chestboat | Real BDS entity inventory through open_entity(). Installed 0.4.0a4 passed named enchanted item deposit, reopen and withdrawal for all three kinds, live hopper pickup, chest-boat movement and source-removal closure. Broader recovery and variant testing remains separate. |
| chestminecart | implemented-tested / native-linked-entity-windows | /workstations open chestminecart | Real BDS entity inventory through open_entity(). Installed 0.4.0a4 passed named enchanted item deposit, reopen and withdrawal for all three kinds, live hopper pickup, chest-boat movement and source-removal closure. Broader recovery and variant testing remains separate. |
| hopperminecart | implemented-tested / native-linked-entity-windows | /workstations open hopperminecart | Real BDS entity inventory through open_entity(). Installed 0.4.0a4 passed named enchanted item deposit, reopen and withdrawal for all three kinds, live hopper pickup, chest-boat movement and source-removal closure. Broader recovery and variant testing remains separate. |
| camel | implemented-unverified / native-linked-equipment-windows | /workstations open camel | The native equipment backend is implemented and its actual vanilla screen opened in the development probe. Native ownership, chest attachment and llama strength determine access and slots. This specific equipment variant has no installed item-transfer qualification. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| camelhusk | implemented-unverified / native-linked-equipment-windows | /workstations open camelhusk | The native equipment backend is implemented and its actual vanilla screen opened in the development probe. Native ownership, chest attachment and llama strength determine access and slots. This specific equipment variant has no installed item-transfer qualification. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| zombiehorse | implemented-unverified / native-linked-equipment-windows | /workstations open zombiehorse | The native equipment backend is implemented and its actual vanilla screen opened in the development probe. Native ownership, chest attachment and llama strength determine access and slots. This specific equipment variant has no installed item-transfer qualification. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| skeletonhorse | unsupported / none | /workstations open skeletonhorse | The pinned vanilla skeleton horse has no inventory/equippable component. Its actual native opener created no manager in the isolated test. An entity type or equipment enum does not create a standalone equipment screen. |
| nautilus | implemented-unverified / native-linked-equipment-windows | /workstations open nautilus | The native equipment backend is implemented and its actual vanilla screen opened in the development probe. Native ownership, chest attachment and llama strength determine access and slots. This specific equipment variant has no installed item-transfer qualification. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| zombienautilus | implemented-unverified / native-linked-equipment-windows | /workstations open zombienautilus | The native equipment backend is implemented and its actual vanilla screen opened in the development probe. Native ownership, chest attachment and llama strength determine access and slots. This specific equipment variant has no installed item-transfer qualification. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| allay | unsupported / none | /workstations open allay | The supplied behavior definitions expose internal item storage used by allay collection and held-item interaction. They do not define a player-openable standalone storage manager; there is no supported remote vanilla screen to expose. |
| panda | unsupported / none | /workstations open panda | The supplied panda inventory is internal held-food state. A player-facing container or trading manager is not defined; remote item inspection would be a custom menu, not a vanilla panda inventory screen. |
| piglin | unsupported / none | /workstations open piglin | The supplied piglin inventory supports internal pickup and barter behavior. Bartering is an interaction, not a villager offer screen; synthesizing merchant offers would change vanilla behavior. |
| agent | implemented-unverified / native-agent-windows | /workstations open agent | Opens the real Agent inventory through its native ContainerComponent. Bedrock owns its 27 slots and transactions. Existing native ownership, source identity and permissions are rechecked; the plugin never assigns ownership or creates an Agent. |
| sign | implemented-unverified / native-sign-windows | /workstations open sign | Real native front/back sign editor and BDS text validation/filtering/save. Two scoped native hooks admit only an authorized remote sign save at the original distance checks. Cancellation revokes the lease, releases the real editor lock using unchanged native state, and restores the client block. |
| npc | implemented-unverified / native-npc-windows | /workstations open npc | Uses the real BDS dialogue command and native NPC scene/actions. Only scene identifiers are translated to unique presentation tokens. Native requests require the matching source, scene, one-use action, source permission and protection guards. BDS executes the installed scene commands; cancellation cannot roll back commands already accepted. |
| commandblock | implemented-unverified / native-command-windows | /workstations open commandblock | Actual command-block editor with native source/window allocation, bounded save decoding and scoped native distance admission. BDS retains command filtering and application; copied deferred saves retain revocable session authority. Requires a Creative operator with administrator and source permissions. |
| commandblockminecart | implemented-unverified / native-command-entity-windows | /workstations open commandblockminecart | Actual Creative GameMode interaction with an authorized live command cart. ActorUniqueID and RuntimeID must resolve the same native source. Bounded saves use BDS filtering/application and revocable copied-payload leases. Default disabled; Creative operator, admin and source permission required. Native riding relationships are read from the source; remote views detach only this client and restore current native links on cleanup. |
| structure | implemented-unverified / native-structure-windows | /workstations open structure | Real structure editor with scoped native source/save ownership, native filtering and template Save/Load/export controls. Display-relative offsets preserve the actual source selection and are converted back before native application. Default disabled and restricted to Creative administrators. |
| jigsaw | implemented-unverified / native-jigsaw-windows | /workstations open jigsaw | Actual native source/window and metadata save through BDS block-actor validation. Default disabled; Creative operator and administrator permission required. Scoped native generation/source/save hooks preserve ordinary edits. Jigsaw structure generation is not part of this metadata editor. |
| compoundcreator | implemented-unverified / native-chemistry-windows | /workstations open compoundcreator | Uses the exact native chemistry manager and reads the actual world Education feature flag. Per-manager distance adaptation retains source and subcontainer validation. BDS owns element controls, recipe validation, input consumption and results. Requires an authorized real table and explicit opt-in. |
| elementconstructor | implemented-unverified / native-chemistry-windows | /workstations open elementconstructor | Uses the exact native chemistry manager and reads the actual world Education feature flag. Per-manager distance adaptation retains source and subcontainer validation. BDS owns element controls, recipe validation, input consumption and results. Requires an authorized real table and explicit opt-in. |
| materialreducer | implemented-unverified / native-chemistry-windows | /workstations open materialreducer | Uses the exact native chemistry manager and reads the actual world Education feature flag. Per-manager distance adaptation retains source and subcontainer validation. BDS owns element controls, recipe validation, input consumption and results. Requires an authorized real table and explicit opt-in. |
| labtable | implemented-unverified / native-chemistry-windows | /workstations open labtable | Uses the exact native chemistry manager and reads the actual world Education feature flag. Per-manager distance adaptation retains source and subcontainer validation. BDS owns element controls, recipe validation, input consumption and results. Requires an authorized real table and explicit opt-in. |
| inventory2x2 | implemented-tested / native-player-inventory-windows | /workstations open inventory2x2 | Opens the actual BDS player inventory, shared by 2x2 crafting, armor, offhand and recipe book. Native 2x2 crafting and an offhand map transfer were exercised; individual tab selection remains client-owned. Cancellation releases plugin tracking; Windows ignores server close packets for this screen, so the player closes it normally before another native UI can open. |
| armor | implemented-unverified / native-player-inventory-windows | /workstations open armor | Opens the actual BDS player inventory, shared by 2x2 crafting, armor, offhand and recipe book. Native 2x2 crafting and an offhand map transfer were exercised; individual tab selection remains client-owned. Cancellation releases plugin tracking; Windows ignores server close packets for this screen, so the player closes it normally before another native UI can open. |
| offhand | implemented-tested / native-player-inventory-windows | /workstations open offhand | Opens the actual BDS player inventory, shared by 2x2 crafting, armor, offhand and recipe book. Native 2x2 crafting and an offhand map transfer were exercised; individual tab selection remains client-owned. Cancellation releases plugin tracking; Windows ignores server close packets for this screen, so the player closes it normally before another native UI can open. |
| cursor | unsupported / none | /workstations open cursor | The cursor is transient state belonging to the active inventory transaction context. It has no standalone window; the native or packet backend owns its validation and cleanup inside the corresponding supported screen. |
| recipebook | implemented-tested / native-player-inventory-windows | /workstations open recipebook | Opens the actual BDS player inventory, shared by 2x2 crafting, armor, offhand and recipe book. Native 2x2 crafting and an offhand map transfer were exercised; individual tab selection remains client-owned. Cancellation releases plugin tracking; Windows ignores server close packets for this screen, so the player closes it normally before another native UI can open. |
| bundle | unsupported / none | /workstations open bundle | DynamicContainer role 63 references item-backed contents with a dynamic identity. Weight limits, nested item rules and ownership are distinct from chest slots; a verified held-bundle executor and save reconciliation are not implemented. |
| hud | unsupported / none | /workstations open hud | The HUD container enum describes the client gameplay layer. It is not a separately openable inventory or workstation. Supported inventory entry points open the actual player inventory instead. |
| cauldron | unsupported / none | /workstations open cauldron | Cauldron enum 9 does not provide a vanilla workstation screen. Filling, dyeing and potion interactions act on the real block and held item; no standalone remote vanilla container is established. |
| jukebox | unsupported / none | /workstations open jukebox | Jukebox enum 17 represents an item-bearing interaction block. Disc insertion/ejection and playback use world interaction, not a standalone player inventory screen. |
| decoratedpot | unsupported / none | /workstations open decoratedpot | Decorated-pot enum 35 and item storage do not provide a standalone vanilla GUI. Insertion and break/drop behavior remain real block interactions; a custom storage menu would be a different interface. |
| fletching | unsupported / none | /workstations open fletching | The pinned vanilla fletching table has no usable crafting GUI or corresponding remote recipe manager. A custom recipe selector may use the dependency menu API, explicitly labeled custom. |
| campfire | unsupported / none | /workstations open campfire | Campfire cooking uses placed world inputs and native block ticking, with no standalone inventory GUI. A furnace screen would change its input, fuel and timing rules. |
| composter | unsupported / none | /workstations open composter | Composting is a real block interaction with native fill probability and extraction. There is no standalone vanilla inventory screen; presenting chest slots would be a custom replacement. |
